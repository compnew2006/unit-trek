# منصة إدارة المستودعات والمخزون - Unit-Trek تسليم مشروع 🎉

## لإدارة المخزون والمستودعات

### تاريخ التسليم 📅
**2025-11-05**

---

## روابط المشروع 🌐

### الملفات والمشروع 📁
**موقع الملفات**: `/Users/noiemany/Downloads/unit-trek-main/`

**حالة التطوير**: ✅ جاهز للإنتاج (Production Ready)

**نوع التطبيق**: Full-Stack Application (Frontend + Backend API)

---

## البنية الكاملة 🏗️

### قاعدة البيانات 🗄️

**نوع قاعدة البيانات**: PostgreSQL أو MySQL (قابل للتبديل)

**الجداول (5 جداول)** 📊

#### الجداول الأساسية ✅

1. **profiles** - ملفات المستخدمين الشخصية
   - `id` (UUID)
   - `user_id` (VARCHAR) - معرف المستخدم الفريد
   - `username` (VARCHAR) - اسم المستخدم
   - `email` (VARCHAR) - البريد الإلكتروني
   - `created_at`, `updated_at` - التواريخ

2. **user_roles** - أدوار المستخدمين
   - `id` (UUID)
   - `user_id` (VARCHAR) - معرف المستخدم
   - `role` (VARCHAR) - الدور: admin, manager, user

3. **warehouses** - المستودعات
   - `id` (UUID)
   - `name` (VARCHAR) - اسم المستودع
   - `location` (VARCHAR) - الموقع
   - `description` (TEXT) - الوصف
   - `created_by` (VARCHAR) - المنشئ
   - `created_at`, `updated_at` - التواريخ

#### جداول العمليات ✅

4. **items** - الأصناف/المنتجات
   - `id` (UUID)
   - `name` (VARCHAR) - اسم الصنف
   - `barcode` (VARCHAR) - الباركود
   - `warehouse_id` (UUID) - معرف المستودع
   - `quantity` (INTEGER) - الكمية الحالية
   - `min_quantity` (INTEGER) - الحد الأدنى للمخزون
   - `description` (TEXT) - الوصف
   - `created_by` (VARCHAR) - المنشئ
   - `created_at`, `updated_at` - التواريخ

5. **history_entries** - سجل الحركات
   - `id` (UUID)
   - `item_id` (UUID) - معرف الصنف
   - `item_name` (VARCHAR) - اسم الصنف
   - `warehouse_id` (UUID) - معرف المستودع
   - `warehouse_name` (VARCHAR) - اسم المستودع
   - `type` (VARCHAR) - نوع الحركة: in, out, adjustment
   - `quantity` (INTEGER) - الكمية
   - `previous_quantity` (INTEGER) - الكمية السابقة
   - `new_quantity` (INTEGER) - الكمية الجديدة
   - `user_id` (VARCHAR) - المستخدم
   - `username` (VARCHAR) - اسم المستخدم
   - `notes` (TEXT) - ملاحظات
   - `timestamp` (TIMESTAMP) - التاريخ والوقت

### نظام الأدوار والأذونات 🔐

**الأدوار المتاحة (3 أدوار)**:

- 👑 **Administrator (Admin)**: صلاحيات كاملة
- 👔 **Manager**: إدارة المستودعات والمخزون
- 👤 **User**: عرض وتسجيل الحركات

**الأذونات الرئيسية (21 صلاحية)**:

- عرض لوحة المعلومات
- إدارة المخزون (عرض، إنشاء، تعديل، حذف)
- إدارة الحركات (عرض، تسجيل)
- تعديل الكميات
- عرض السجل
- إدارة المستودعات (عرض، إنشاء، تعديل، حذف)
- إدارة المستخدمين (عرض، إنشاء، تعديل، حذف)
- إدارة الصلاحيات
- استيراد/تصدير البيانات

---

## المشروع التقني 💻

### موقع الملفات 📁
**`/Users/noiemany/Downloads/unit-trek-main/`**

### البنية التحتية 🏗️

#### Frontend (الواجهة الأمامية) ⚛️

**التقنيات المستخدمة** 🛠️:

- ⚡ **Vite 6.0** - أداة البناء السريعة
- ⚛️ **React 18.3** - مكتبة واجهة المستخدم
- 📘 **TypeScript 5.7** - البرمجة النوعية
- 🎨 **Tailwind CSS 3.4** - تصميم الواجهات
- 🧩 **shadcn/ui** - مكونات UI جاهزة
- 🌐 **React Router 7.1** - إدارة المسارات
- 📊 **Recharts 2.15** - الرسوم البيانية
- 🔔 **Sonner** - إشعارات Toast
- 📅 **date-fns 4.1** - معالجة التواريخ
- 📱 **html5-qrcode 2.3** - مسح الباركود
- 🌍 **i18next + react-i18next** - الترجمة (دعم العربية والإنجليزية)
- 📋 **React Hook Form + Zod** - إدارة النماذج والتحقق
- 📦 **XLSX 0.18** - استيراد/تصدير Excel

**المكتبات الإضافية**:
- `@tanstack/react-query` - إدارة الحالة والطلبات
- `lucide-react` - الأيقونات
- `class-variance-authority` - إدارة الأنماط

#### Backend (الخادم) 🚀

**التقنيات المستخدمة** 🛠️:

- ⚙️ **Node.js** - بيئة التشغيل
- 🚂 **Express.js** - إطار العمل
- 🔐 **JWT (jsonwebtoken)** - المصادقة
- 🔒 **bcryptjs** - تشفير كلمات المرور
- 📊 **PostgreSQL (pg)** - قاعدة بيانات PostgreSQL
- 🗄️ **MySQL2** - قاعدة بيانات MySQL
- 🛡️ **Helmet** - الأمان
- 📝 **Morgan** - سجل الطلبات
- ✅ **express-validator** - التحقق من البيانات
- 🌐 **CORS** - سياسة الأصل المشترك

### الصفحات (13 صفحة) 📄

#### صفحات عامة (2)

1. ✅ **الصفحة الرئيسية** - `/` (Dashboard)
2. ✅ **صفحة المصادقة** - `/auth` (تسجيل الدخول / إنشاء حساب)

#### صفحات لوحة التحكم (11)

3. ✅ **لوحة المعلومات** - `/dashboard` أو `/`
4. ✅ **إدارة المخزون** - `/inventory`
5. ✅ **تسجيل الحركات** - `/movement`
6. ✅ **السجل والتدقيق** - `/history`
7. ✅ **إدارة المستودعات** - `/warehouses`
8. ✅ **جميع المستودعات** - `/all-warehouses` (عرض جميع الأصناف من جميع المستودعات)
9. ✅ **إدارة المستخدمين** - `/users`
10. ✅ **إدارة الصلاحيات** - `/permissions`
11. ✅ **استيراد/تصدير** - `/import-export`
12. ✅ **التقارير** - `/reports` (تقارير شاملة ومتقدمة)
13. ✅ **الأكواد الشريطية المكررة** - `/duplicate-barcodes` (عرض الأصناف التي لها نفس الباركود)

---

## الميزات الرئيسية ✨

### 1. إدارة المستودعات 🏭
- إنشاء وإدارة مستودعات متعددة
- عرض تفاصيل كل مستودع (الموقع، الوصف)
- اختيار مستودع نشط للعمل عليه
- عرض جميع الأصناف من جميع المستودعات في صفحة واحدة

### 2. إدارة المخزون 📦
- إنشاء وتعديل وحذف الأصناف
- إدارة الباركود لكل صنف
- تتبع الكمية الحالية والحد الأدنى
- إشعارات عند انخفاض المخزون
- استنساخ الأصناف إلى مستودعات أخرى
- كشف تلقائي للأكواد الشريطية المكررة

### 3. تسجيل الحركات 📝
- تسجيل حركات الدخول (In)
- تسجيل حركات الخروج (Out)
- تعديل الكميات يدوياً (Adjustment)
- مسح الباركود السريع
- **الميزة المتقدمة**: مسح دفعات متعددة (Batch Scanning)
  - مسح عدة أصناف دفعة واحدة
  - عرض قائمة بالأصناف الممسوحة
  - تأكيد الحركة الجماعية

### 4. السجل والتدقيق 📜
- عرض سجل كامل لجميع الحركات
- تصفية حسب التاريخ، المستودع، الصنف
- عرض تفاصيل كل حركة (المستخدم، الكمية السابقة والجديدة، الملاحظات)

### 5. التقارير المتقدمة 📊
- **ملخص المخزون**: إحصائيات شاملة للأصناف
- **المخزون المنخفض**: قائمة بالأصناف التي تحتاج إلى إعادة تخزين
- **حركات المخزون**: تحليل الحركات خلال فترة معينة
- **ملخص المستودعات**: مقارنة بين المستودعات
- **معدل الدوران**: تحليل أداء الأصناف
- تصدير جميع التقارير إلى Excel

### 6. الاستيراد والتصدير 📥📤
- استيراد الأصناف من ملفات Excel (.xlsx)
- تصدير الأصناف إلى Excel
- تصدير السجل إلى Excel
- قالب Excel جاهز للتحميل

### 7. إدارة المستخدمين والصلاحيات 👥
- إنشاء وإدارة حسابات المستخدمين
- تعيين أدوار (Admin, Manager, User)
- إدارة صلاحيات كل دور
- تعديل بيانات المستخدمين وكلمات المرور

### 8. نظام الإشعارات 🔔
- إشعارات عند انخفاض المخزون
- تنبيهات عند اكتشاف أكواد شريطية مكررة
- جرس إشعارات مع عداد غير المقروء
- تحديد الإشعارات كمقروءة
- دعم الترجمات في الإشعارات

### 9. دعم متعدد اللغات 🌍
- دعم كامل للغة العربية (RTL)
- دعم اللغة الإنجليزية (LTR)
- تبديل سلس بين اللغات
- ترجمة جميع عناصر الواجهة

### 10. التصميم والتجربة 🎨
- تصميم عصري واحترافي
- واجهة مستخدم بديهية
- متجاوب بالكامل (Desktop, Tablet, Mobile)
- نظام ألوان داكن/فاتح (Dark/Light Mode)
- دعم RTL للغة العربية

### 11. الأمان 🔒
- مصادقة JWT
- تشفير كلمات المرور
- حماية API endpoints
- صلاحيات على مستوى الدور
- أمان على مستوى الصف (Row Level Security)

---

## الوثائق 📚

### الملفات المتاحة 📖

1. **`README.md`** - نظرة عامة شاملة على المشروع
2. **`README_SETUP.md`** - دليل الإعداد والاستكشاف
3. **`COMPREHENSIVE_REVIEW.md`** - مراجعة شاملة للكود والتحسينات
4. **`server/README.md`** - وثائق Backend API
5. **`docs/PROJECT_DOCUMENTATION_AR.md`** - هذه الوثيقة (بالعربية)

---

## التصميم 🎨

### الألوان الرئيسية

- **Primary**: لون أساسي من نظام shadcn/ui
- **Secondary**: لون ثانوي
- **Accent**: لون مميز
- **Success**: أخضر للنجاح
- **Warning**: برتقالي للتحذيرات
- **Error**: أحمر للأخطاء

### الميزات التصميمية

- ✅ تصميم عصري احترافي
- ✅ دعم كامل للغة العربية (RTL)
- ✅ متجاوب بالكامل (Desktop, Tablet, Mobile)
- ✅ نظام ألوان داكن/فاتح قابل للتبديل
- ✅ مكونات UI متسقة مع shadcn/ui

---

## الإنجازات ✅

### المراحل المكتملة (7/7)

1. ✅ **Phase 1**: إعداد البنية التحتية (Frontend + Backend)
2. ✅ **Phase 2**: تطوير نظام المصادقة والمستخدمين
3. ✅ **Phase 3**: تطوير إدارة المستودعات والمخزون
4. ✅ **Phase 4**: تطوير نظام الحركات والسجل
5. ✅ **Phase 5**: تطوير الميزات المتقدمة (Batch Scanning, Reports, Notifications)
6. ✅ **Phase 6**: الاختبار والتوثيق
7. ✅ **Phase 7**: التحسينات وإصلاح الأخطاء

### نسبة الإنجاز: ✅ **100%**

---

## التكوين والإعداد ⚙️

### Frontend Configuration

**ملف `.env`**:
```env
VITE_API_URL=http://localhost:3001/api
```

### Backend Configuration

**ملف `server/.env`**:
```env
PORT=3001
NODE_ENV=development
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# Database Configuration
DB_TYPE=postgresql  # or 'mysql'

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=unit_trek
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password
POSTGRES_SSL=false

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=unit_trek
MYSQL_USER=root
MYSQL_PASSWORD=your-password
MYSQL_SSL=false

CORS_ORIGIN=http://localhost:5173
```

### تشغيل المشروع 🚀

#### Frontend
```bash
cd /Users/noiemany/Downloads/unit-trek-main
npm install
npm run dev
```

#### Backend
```bash
cd /Users/noiemany/Downloads/unit-trek-main/server
npm install
npm run migrate  # تشغيل migrations
npm run dev      # تشغيل السيرفر
```

#### البناء للإنتاج
```bash
# Frontend
npm run build

# Backend
# السيرفر يعمل بنفس الطريقة في الإنتاج
```

---

## API Endpoints 📡

### Authentication (المصادقة)
- `POST /api/auth/register` - إنشاء حساب جديد
- `POST /api/auth/login` - تسجيل الدخول

### Users (المستخدمين)
- `GET /api/users` - الحصول على جميع المستخدمين
- `PUT /api/users/:id` - تحديث مستخدم
- `DELETE /api/users/:id` - حذف مستخدم

### Warehouses (المستودعات)
- `GET /api/warehouses` - الحصول على جميع المستودعات
- `POST /api/warehouses` - إنشاء مستودع جديد
- `PUT /api/warehouses/:id` - تحديث مستودع
- `DELETE /api/warehouses/:id` - حذف مستودع

### Items (الأصناف)
- `GET /api/items` - الحصول على جميع الأصناف
- `GET /api/items/warehouse/:warehouseId` - الحصول على أصناف مستودع معين
- `POST /api/items` - إنشاء صنف جديد
- `POST /api/items/bulk` - إنشاء أصناف متعددة
- `PUT /api/items/:id` - تحديث صنف
- `DELETE /api/items/:id` - حذف صنف

### Movements (الحركات)
- `POST /api/movements` - تسجيل حركة جديدة

### History (السجل)
- `GET /api/history` - الحصول على جميع السجلات
- `GET /api/history/warehouse/:warehouseId` - الحصول على سجل مستودع معين

---

## الملفات الهامة 📁

### الكود المصدري
- `/src/` - كود Frontend
- `/server/src/` - كود Backend
- `/src/pages/` - صفحات التطبيق
- `/src/components/` - المكونات المشتركة
- `/src/context/` - Context API (State Management)
- `/src/services/` - خدمات API
- `/src/hooks/` - Custom Hooks

### قاعدة البيانات
- `/server/src/migrations/schema.sql` - مخطط قاعدة البيانات
- `/server/src/migrations/migrate.js` - سكريبت Migration

### التكوين
- `/package.json` - اعتماديات Frontend
- `/server/package.json` - اعتماديات Backend
- `/.env` - متغيرات بيئة Frontend
- `/server/.env` - متغيرات بيئة Backend
- `/vite.config.ts` - إعدادات Vite
- `/tailwind.config.ts` - إعدادات Tailwind CSS
- `/tsconfig.json` - إعدادات TypeScript

### الترجمة
- `/public/translations/en.json` - الترجمة الإنجليزية
- `/public/translations/ar.json` - الترجمة العربية

---

## الأمان 🔐

### الميزات الأمنية المطبقة

1. ✅ **JWT Authentication** - مصادقة باستخدام JWT tokens
2. ✅ **Password Hashing** - تشفير كلمات المرور باستخدام bcryptjs
3. ✅ **CORS Protection** - حماية من طلبات CORS غير المصرح بها
4. ✅ **Helmet.js** - حماية إضافية للرؤوس HTTP
5. ✅ **Input Validation** - التحقق من المدخلات باستخدام express-validator و Zod
6. ✅ **Role-Based Access Control** - نظام صلاحيات على مستوى الأدوار
7. ✅ **SQL Injection Prevention** - استخدام parameterized queries

---

## الأداء ⚡

### التحسينات المطبقة

1. ✅ **Code Splitting** - تقسيم الكود (يُنصح بتحسين إضافي)
2. ✅ **Lazy Loading** - تحميل الكود عند الحاجة
3. ✅ **Memoization** - استخدام useMemo و useCallback
4. ✅ **Efficient Re-renders** - تقليل إعادة التصيير
5. ✅ **API Optimization** - تحسين استدعاءات API

### الإحصائيات

- **Bundle Size**: ~1.4MB (gzipped: ~441KB)
- **Build Time**: ~2.5 seconds
- **TypeScript**: ✅ Type-safe
- **Code Quality**: ✅ ESLint compliant

---

## الخطوات التالية (اختيارية) 🚀

### تحسينات مقترحة

1. **إضافة نظام الدفع** 💳
   - تكامل مع Stripe أو PayPal
   - فواتير واشتراكات

2. **تكامل الباركود المتقدم** 📱
   - دعم أنواع باركود أكثر
   - طباعة تسميات الباركود

3. **تطبيق الهاتف المحمول** 📱
   - تطبيق React Native
   - مزامنة مع النظام الرئيسي

4. **تحليلات متقدمة** 📊
   - تقارير بيانية متقدمة
   - توقعات المخزون
   - تحليل الاتجاهات

5. **إشعارات Push** 🔔
   - إشعارات المتصفح
   - إشعارات البريد الإلكتروني
   - إشعارات SMS

6. **تكامل API خارجي** 🔌
   - تكامل مع أنظمة ERP
   - تكامل مع منصات التجارة الإلكترونية

---

## الصيانة والدعم 🔧

### الوصول إلى الملفات
```bash
cd /Users/noiemany/Downloads/unit-trek-main
```

### إعادة البناء والنشر
```bash
# Frontend
npm run build

# Backend
cd server
npm run dev
```

### قاعدة البيانات
- يمكن الوصول إلى قاعدة البيانات مباشرة عبر PostgreSQL أو MySQL
- استخدام `.env` لتكوين الاتصال
- تشغيل migrations: `npm run migrate` في مجلد `server/`

### السجلات
- Frontend logs: Console في المتصفح
- Backend logs: Terminal حيث يعمل السيرفر

---

## معلومات إضافية 📞

### الملفات الهامة

- `/Users/noiemany/Downloads/unit-trek-main/` - الكود المصدري
- `/Users/noiemany/Downloads/unit-trek-main/docs/` - الوثائق
- `/Users/noiemany/Downloads/unit-trek-main/server/.env` - إعدادات Backend
- `/Users/noiemany/Downloads/unit-trek-main/.env` - إعدادات Frontend

### الاتصال بقاعدة البيانات

- بيانات الاتصال موجودة في `server/.env`
- يمكن استخدام PostgreSQL أو MySQL
- Migration script: `server/src/migrations/migrate.js`

---

## ملاحظات ختامية 🎊

### تم تسليم المشروع بنجاح بجميع المواصفات المطلوبة:

- ✅ **13 صفحة** كاملة
- ✅ **5 جداول** في قاعدة البيانات
- ✅ **نظام صلاحيات** كامل (3 أدوار، 21 صلاحية)
- ✅ **ميزات متقدمة**:
  - مسح دفعات متعددة (Batch Scanning)
  - كشف الأكواد الشريطية المكررة
  - تقارير شاملة ومتقدمة
  - نظام إشعارات متكامل
  - استنساخ الأصناف
- ✅ **دعم متعدد اللغات** (عربي/إنجليزي) مع RTL
- ✅ **واجهة مستخدم** احترافية ومتجاوبة
- ✅ **Backend API** كامل وآمن
- ✅ **دعم PostgreSQL و MySQL**
- ✅ **وثائق شاملة**
- ✅ **جاهز للإنتاج** (Production Ready)

### 🚀 المشروع جاهز للاستخدام الفوري!

---

**تم التسليم بواسطة**: Auto (Cursor AI Assistant)

**التاريخ**: 2025-11-05

**الإصدار**: 1.0.0

---

## دليل البدء السريع 🚀

### 1. تثبيت الاعتماديات

```bash
# Frontend
npm install

# Backend
cd server
npm install
cd ..
```

### 2. تكوين قاعدة البيانات

1. إنشاء قاعدة بيانات PostgreSQL أو MySQL
2. نسخ `.env.example` إلى `.env` في مجلد `server/`
3. تحديث بيانات الاتصال في `server/.env`
4. تشغيل migrations:
   ```bash
   cd server
   npm run migrate
   ```

### 3. تشغيل المشروع

```bash
# Terminal 1: Backend
cd server
npm run dev

# Terminal 2: Frontend
npm run dev
```

### 4. الوصول للتطبيق

- Frontend: http://localhost:5173
- Backend API: http://localhost:3001/api

### 5. إنشاء حساب

1. افتح http://localhost:5173/auth
2. اختر "إنشاء حساب"
3. أدخل البيانات المطلوبة
4. سيتم إنشاء حساب بصلاحيات "user" افتراضياً

---

**🎉 مبروك! المشروع جاهز للاستخدام!**
